// Acccept a list of user player addresses
// Secret for each user and their roles
// Create a public commitment with a hash of each user address, role and secret
// The list of player addresses is a public input